name: Check Preinstalled Tools

on: push

jobs:
  check-tools:
    runs-on: ubuntu-latest

    steps:
      - name: Print tool versions
        run: |
          echo "Docker version:"
          docker --version

          echo "Python version:"
          python --version

          echo "Node version:"
          node --version

          echo "Git version:"
          git --version
